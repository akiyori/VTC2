#include "party.h"

//Party::Party(int affiliationId, int id, const std::string & name)
//{
//	CharacterManager::CharacterManager(id, name);
//	this->affiliationId = affiliationId;
//
//	int count = 10;
//	characters.reserve(count);
//	for (int i = 0; i < count; i++) {
//		characters.push_back(new Character(affiliationId, id));
//	}
//}
